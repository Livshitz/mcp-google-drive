{
	"mcpServers": {
		"mcp-google-drive": {
			"type": "stdio",
			"command": "bun",
			"args": ["run", "src/mcp/cli.ts", "--stdio"],
			"cwd": "${workspaceFolder}",
			"envFile": "${workspaceFolder}/.env"
		}
	}
}
